<nav class="navbar navbar-toggleable-md navbar-light bg-faded">
    <div class="container">
      <div class="navbar-header" style='margin-top: 10px; font-size: 25px;'>
        <a href="/" >Shoppit</a>
      </div>
      <form class="navbar-form navbar-left" action="/search" method="post">
        <div class="form-group">
          <input type="text" name="search_term_nav_bar" value="" class='form-control' placeholder="search...">
          <button type="submit" class='btn btn-default' name="button">Search</button>
        </div>
      </form>
      <ul class='nav navbar-nav navbar-right'>

        <% if (user && user.isAdmin) { %>
          <li><a href="#" class='dropdown-toggle' data-toggle='dropdown'>Admin Dashboard</a>
            <ul class='dropdown-menu'>
            <li><a href="/add-category">Add Categories</a></li>
            <li><a href="/add-product">Add Products</a></li>
            </ul>
          </li>
        <% } %>

        <li><a href="#" class='dropdown-toggle' data-toggle='dropdown'>Categories</a>
          <ul class='dropdown-menu'>
            <% for(var i = 0; i < categories.length; i++) { %>
              <li><a href='/products/<%= categories[i]._id%>'><%= categories[i].name %></a></li>
            <% } %>
          </ul>
        </li>
        <% if (!user) { %>
        <li><a href="#" class='dropdown-toggle' data-toggle='dropdown'>Sign Up/Login</a>
          <ul class='dropdown-menu'>
            <li><a href="/signup">Sign Up</a></li>
            <li><a href="/login">Log In</a></li>
            <li><a href="/adminlogin">Admin Log In</a></li>
          </ul>
        </li>
        <% } else { %>

          <li><a href="#" class='dropdown-toggle' data-toggle='dropdown'><%= user.profile.name[0].toUpperCase() + user.profile.name.slice(1)%></a>
            <ul class='dropdown-menu'>
              <li><a href="/profile">Profile</a></li>
              <li><a href="/logout">Log Out</a></li>
            </ul>
          </li>
          <% } %>



          <% if (user && !user.isAdmin) { %>
          <li><a href="/cart" class='glyphicon glyphicon-shopping-cart'><span class='badge'><%= cart %></span></a></li>
          <% }  else {%>
            <li></li>
            <% } %>


      </ul>




    </div>
</nav>
